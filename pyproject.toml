[project]
name = "datalens"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["fastapi", "uvicorn", "pandas", "python-multipart", "google-generativeai", "python-dotenv", "httpx", "pydantic"]
